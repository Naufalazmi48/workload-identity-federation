{
  "name": "workload-identity-federation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:storage": "functions-framework --target=getPublicUrl --port=8000",
    "start:calendar": "functions-framework --target=createCalendar --port=8001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "22"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "@google-cloud/local-auth": "^2.1.0",
    "@google-cloud/storage": "^7.7.0",
    "googleapis": "^105.0.0"
  }
}
